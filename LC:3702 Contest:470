class Solution {
    public int longestSubsequence(int[] nums) {
        int n = nums.length;
        
        int xor = 0;
        for(var el : nums) xor ^= el;
        if(xor != 0) return n;

        for(var el : nums) {
            if(el != 0) return n-1;
        }

        return 0;
    }
}
